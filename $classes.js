/**
 * Definition for singly-linked list.
 */
export class ListNode {
  /**
   * @param { * } val
   * @param { ListNode } next
   */
  constructor (val, next) {
    this.val = val
    this.next = next
  }
}
